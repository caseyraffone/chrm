{
  "name": "chrm-backend",
  "version": "0.1.0",
  "private": true,
  "description": "CHRM backend — proxies OpenAI/Anthropic calls so API keys never ship in the client. Deploys as Vercel serverless functions.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "dev": "vercel dev"
  },
  "dependencies": {
    "formidable": "^3.5.1"
  }
}
